export enum Role {
  ADMIN = 'admin',
  DIRECTOR = 'director',
  MANAGER = 'manager',
  SUPPORT = 'support',
  SELLER = 'seller',
  MARKETER = 'marketer',
  CLIENT = 'client',
  PARTNER = 'partner',
}
